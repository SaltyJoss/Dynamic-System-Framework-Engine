// DSFE_GUI ProjectPage.cpp
#include "Workspace/ProjectPage.h"

namespace Workspace {
	ProjectPage::ProjectPage(QWidget* parent) : QWidget(parent) {
		// Set up the UI for the project page here (e.g., using QVBoxLayout, adding widgets, etc.)
	}
} // namespace Workspace